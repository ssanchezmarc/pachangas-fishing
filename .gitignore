# Dependencies
/node_modules

# Next.js build output
/.next
/out

# Production
/build

# Environment (secrets — never commit)
.env
.env.local
.env*.local

# TypeScript build info
*.tsbuildinfo
next-env.d.ts

# Supabase local
/supabase/.branches
/supabase/.temp

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.vscode
.idea

# Scratch (local planning notes)
/.scratch

# AI reader fixtures — real plica photos with handwritten signatures (PII);
# kept locally to run the gated #07 fixture test, never committed.
/src/lib/ai/fixtures/
.vercel
